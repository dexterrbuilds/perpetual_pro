# perpetual_pro — production dependencies for Render / local
# Pure-pandas indicator fallback is always available (no pandas-ta required at boot).

python-3.12.0
ccxt>=4.2.0
pandas>=2.0.0
numpy>=1.24.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
rich>=13.7.0
requests>=2.31.0
pyyaml>=6.0.1
loguru>=0.7.2
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Local / desktop only (not required for API on Render):
# mss>=9.0.0
# easyocr>=1.7.0
# Optional enhanced indicators (import is optional; app boots without it):
# pandas-ta-classic>=0.4.0
