# perpetual_pro — production dependencies (Render-safe)
#
# Indicator backend: pandas-ta-classic (import name: pandas_ta_classic).
# App still boots if install fails (pure-pandas fallback in indicators.py).

ccxt>=4.2.0
pandas>=2.0.0,<3
numpy>=1.24.0,<3
pandas-ta-classic>=0.4.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
rich>=13.7.0
requests>=2.31.0
pyyaml>=6.0.1
loguru>=0.7.2
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
streamlit>=1.39.0

# Desktop-only (local CLI screen capture) — not required on Render:
# mss>=9.0.0
# easyocr>=1.7.0
